type_info:
  documentationLink: https://developers.google.com/storage/docs/json_api/
  kind: deploymentmanager#typeInfo
  name: projects.serviceAccount
  schema:
    input: '{}'
    output:
      $schema: http://json-schema.org/draft-03/schema#
      mainSchema:
        $ref: '#/schemas/ServiceAccount'
      methodName: get
      schemas:
        ServiceAccount:
          description: A subscription to receive Google PubSub notifications.
          id: ServiceAccount
          properties:
            email_address:
              description: The ID of the notification.
              type: string
            kind:
              default: storage#serviceAccount
              description: The kind of item this is. For notifications, this is always
                storage#notification.
              type: string
          type: object
  selfLink: https://www.googleapis.com/deploymentmanager/v2beta/projects/gcp-types/global/typeProviders/storage-v1/types/projects.serviceAccount?alt=json
  title: Cloud Storage JSON API
